diff --git i/crates/amon-detect/src/vendor/detect/mod.rs w/crates/amon-detect/src/vendor/detect/mod.rs
index ec499b1..e5e6bc3 100644
--- i/crates/amon-detect/src/vendor/detect/mod.rs
+++ w/crates/amon-detect/src/vendor/detect/mod.rs
@@ -1183,6 +1183,7 @@ mod tests {
 
     #[cfg(target_os = "linux")]
     #[test]
+    #[ignore = "amon stubs out foreground process scanning; it identifies agents from argv"]
     fn foreground_job_detects_sleep() {
         use portable_pty::CommandBuilder;
 
@@ -1215,6 +1216,7 @@ mod tests {
 
     #[cfg(target_os = "linux")]
     #[test]
+    #[ignore = "amon stubs out foreground process scanning; it identifies agents from argv"]
     fn foreground_job_detects_shell_running_command() {
         use portable_pty::CommandBuilder;
         use std::io::Write;
@@ -1251,6 +1253,7 @@ mod tests {
 
     #[cfg(target_os = "linux")]
     #[test]
+    #[ignore = "amon stubs out foreground process scanning; it identifies agents from argv"]
     fn foreground_job_detects_agent_behind_shell_wrapper() {
         use portable_pty::CommandBuilder;
 
