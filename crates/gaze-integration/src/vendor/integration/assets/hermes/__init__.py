# Generated by scripts/revendor.sh from herdr — do not edit by hand.
# Upstream: https://github.com/ogulcancelik/herdr/blob/514e4465ee33d4d81682d06ee2934483982a54ed/src/integration/assets/hermes/__init__.py
# Copyright Ogulcan Celik, licensed under the Apache License 2.0.
# Modified only by the mechanical token map in scripts/revendor.sh.

"""Hermes plugin installed by Gaze to report resumable session identity."""

# GAZE_INTEGRATION_ID=hermes
# GAZE_INTEGRATION_VERSION=4

from __future__ import annotations

import json
import os
import random
import socket
import time

_SOURCE = "gaze:hermes"
_AGENT = "hermes"
_INTERACTIVE_PLATFORMS = {"cli", "tui", "desktop", "acp"}


def _base_params() -> tuple[str, str] | None:
    if os.environ.get("GAZE_ENV") != "1":
        return None
    agent_id = os.environ.get("GAZE_AGENT_ID", "").strip()
    socket_path = os.environ.get("GAZE_SOCKET_PATH", "").strip()
    if not agent_id or not socket_path:
        return None
    return agent_id, socket_path


def _send(method: str, params: dict) -> None:
    base = _base_params()
    if base is None:
        return
    agent_id, socket_path = base
    params = {
        "agent_id": agent_id,
        "source": _SOURCE,
        "agent": _AGENT,
        "seq": time.time_ns(),
        **params,
    }
    request = {
        "id": f"{_SOURCE}:{int(time.time() * 1000)}:{random.randrange(1_000_000):06d}",
        "method": method,
        "params": params,
    }
    try:
        client = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
        client.settimeout(0.5)
        client.connect(socket_path)
        client.sendall((json.dumps(request) + "\n").encode("utf-8"))
        try:
            client.recv(4096)
        except Exception:
            pass
        client.close()
    except Exception:
        pass


def _report_session(start_source: str, **kwargs) -> None:
    if kwargs.get("platform") not in _INTERACTIVE_PLATFORMS:
        return
    session_id = kwargs.get("session_id")
    if not isinstance(session_id, str) or not session_id:
        return
    _send(
        "agent.report_session",
        {
            "agent_session_id": session_id,
            "session_start_source": start_source,
        },
    )


def _session_started(**kwargs) -> None:
    _report_session("startup", **kwargs)


def _session_reset(**kwargs) -> None:
    _report_session("new", **kwargs)


def _session_observed(**kwargs) -> None:
    if kwargs.get("platform") == "cli":
        _report_session("resume", **kwargs)


def register(ctx):
    ctx.register_hook("on_session_start", _session_started)
    ctx.register_hook("on_session_reset", _session_reset)
    ctx.register_hook("pre_llm_call", _session_observed)
