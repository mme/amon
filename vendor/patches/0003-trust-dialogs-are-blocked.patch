diff --git i/crates/amon-detect/src/vendor/detect/manifests/claude.toml w/crates/amon-detect/src/vendor/detect/manifests/claude.toml
index 01e2932..749f069 100644
--- i/crates/amon-detect/src/vendor/detect/manifests/claude.toml
+++ w/crates/amon-detect/src/vendor/detect/manifests/claude.toml
@@ -4,7 +4,9 @@
 # Modified only by the mechanical token map in scripts/revendor.sh.
 
 id = "claude"
-version = "2026.07.13.1"
+# amon vendor/patches/0003 bumps the patch version so the added rule outranks
+# an already-cached remote manifest of the unpatched version.
+version = "2026.07.13.2"
 min_engine_version = 2
 updated_at = "2026-07-13T00:00:00Z"
 aliases = ["claude-code"]
@@ -66,6 +68,17 @@ region = "whole_recent"
 visible_blocker = true
 contains = ["run a dynamic workflow?", "esc to cancel"]
 
+# amon vendor/patches/0003: upstream misses the first-run folder-trust
+# dialog, which needs a human. A newer remote manifest from herdr.dev
+# overrides this rule until the fix lands upstream (ADR-0003).
+[[rules]]
+id = "trust_dialog_blocked"
+state = "blocked"
+priority = 950
+region = "whole_recent"
+visible_blocker = true
+contains = ["do you trust"]
+
 [[rules]]
 id = "live_prompt_box"
 state = "idle"
diff --git i/crates/amon-detect/src/vendor/detect/manifests/codex.toml w/crates/amon-detect/src/vendor/detect/manifests/codex.toml
index 1995de0..ce09677 100644
--- i/crates/amon-detect/src/vendor/detect/manifests/codex.toml
+++ w/crates/amon-detect/src/vendor/detect/manifests/codex.toml
@@ -4,7 +4,9 @@
 # Modified only by the mechanical token map in scripts/revendor.sh.
 
 id = "codex"
-version = "2026.07.18.1"
+# amon vendor/patches/0003 bumps the patch version so the added rule outranks
+# an already-cached remote manifest of the unpatched version.
+version = "2026.07.18.2"
 min_engine_version = 2
 updated_at = "2026-07-18T00:00:00Z"
 
@@ -61,6 +63,17 @@ any = [
   { contains = ["would you like to"], any = [{ contains = ["yes"] }, { contains = ["❯"] }] },
 ]
 
+# amon vendor/patches/0003: upstream misses the first-run directory-trust
+# dialog, which needs a human. A newer remote manifest from herdr.dev
+# overrides this rule until the fix lands upstream (ADR-0003).
+[[rules]]
+id = "trust_dialog_blocked"
+state = "blocked"
+priority = 950
+region = "whole_recent"
+visible_blocker = true
+contains = ["do you trust"]
+
 [[rules]]
 id = "screen_working_fallback"
 state = "working"
